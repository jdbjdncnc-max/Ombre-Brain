{
  "name": "ombre-brain-mobile",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Personal Android shell for the web-first Ombre Brain companion.",
  "scripts": {
    "web:prepare": "node scripts/prepare-web.mjs",
    "test": "node scripts/prepare-web.mjs && node --test tests/*.test.mjs",
    "cap:add:android": "npm run web:prepare && cap add android",
    "cap:sync": "npm run web:prepare && cap sync android",
    "cap:open": "cap open android",
    "cap:doctor": "cap doctor android",
    "android:build:debug": "npm run cap:sync && cd android && gradlew.bat assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "8.5.0",
    "@capacitor/core": "8.5.0"
  },
  "devDependencies": {
    "@capacitor/cli": "8.5.0"
  },
  "engines": {
    "node": ">=22"
  }
}
